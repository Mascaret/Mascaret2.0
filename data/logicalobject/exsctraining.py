# Kivy libs import

# Python libs import

# Personal libs import

#Class of a task
class ExScTraining:

    # columns used in the csv, in the right order
    _dtb_columns = [
                "exercisescaling_id", "training_id", "exercisescaling_n"
                   ]
    # name of the dtb table for this object
    _dtb_table = "exsctrainings"
    # Object(s) which must be included in this object
    _parent_objct = []
