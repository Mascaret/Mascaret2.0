# Kivy libs import

# Python libs import

# Personal libs import

#Class of a task
class Location:

    # columns used in the csv, in the right order
    _dtb_columns = ["location_id", "location_name", "legalentity_id"]
    # name of the dtb table for this object
    _dtb_table = "locations"
    # Object(s) which must be included in this object
    _parent_objct = []
