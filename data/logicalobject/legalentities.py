# Kivy libs import

# Python libs import

# Personal libs import

#Class of a task
class LegalEntity:

    # columns used in the csv, in the right order
    _dtb_columns = ["legalentity_id", "legalentity_name"]
    # name of the dtb table for this object
    _dtb_table = "legalentities"
    # Object(s) which must be included in this object
    _parent_objct = []
